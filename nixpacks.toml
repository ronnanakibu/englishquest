[phases.install]
cmds = ["npm install -g pnpm", "cd apps/api && pnpm install --no-frozen-lockfile"]

[phases.build]
cmds = ["cd apps/api && npx prisma generate && pnpm run build"]

[start]
cmd = "cd apps/api && node dist/server.js"
